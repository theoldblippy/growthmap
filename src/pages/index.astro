---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .filter((post) => !post.data.draft && !post.data.guide)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---

<Layout
  title="Growth Map | Fractional COO for Owner-Led Businesses, $1M–$50M"
  description="Your business can't grow past you. Growth Map finds what's capping growth in your own numbers — not a workshop — then builds the system that runs without you. Fractional COO for owner-led companies, $1M–$50M."
  canonicalPath="/"
>
  <!-- HERO -->
  <section class="relative overflow-hidden border-b border-hairline px-6 py-16 md:py-24">
    <svg
      class="pointer-events-none absolute -right-36 -top-16 h-[640px] w-[640px] text-moss opacity-45"
      viewBox="0 0 600 600"
      aria-hidden="true"
    >
      <g fill="none" stroke="currentColor" stroke-width="1.2" opacity="0.75">
        <path d="M300,80 C420,70 520,160 530,280 C540,400 460,520 330,530 C200,540 80,460 75,320 C70,180 180,90 300,80 Z" />
        <path d="M300,130 C390,122 470,190 478,285 C486,380 420,470 320,478 C220,486 128,420 124,310 C120,200 210,138 300,130 Z" />
        <path d="M300,180 C365,174 420,222 426,290 C432,358 385,420 312,426 C239,432 176,384 173,305 C170,226 235,186 300,180 Z" />
        <path d="M300,230 C340,226 368,254 372,295 C376,336 348,372 305,375 C262,378 226,350 224,302 C222,254 260,234 300,230 Z" />
        <circle cx="299" cy="301" r="5" fill="currentColor" stroke="none" />
      </g>
    </svg>

    <div class="relative z-10 mx-auto grid max-w-[1200px] items-center gap-12 md:grid-cols-[1.25fr_0.75fr] md:gap-14">
      <div>
        <h1 class="font-display text-4xl leading-[1.12] text-ink sm:text-5xl md:text-[56px]">
          Your business can't grow past <em>you.</em>
        </h1>
        <p class="mt-6 max-w-[48ch] text-lg leading-relaxed text-fern">
          Every decision still runs through you. I find what's holding growth back in your own numbers — not a workshop — then build the system that runs without you.
        </p>
        <div class="mt-8 flex flex-wrap items-center gap-4">
          <a
            href="https://calendar.notion.so/meet/cameronearloliver/qc3y04oud"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-block rounded-full border border-ink bg-ink px-7 py-3.5 text-[15px] font-semibold text-parchment transition-opacity hover:opacity-90"
          >
            Book a Free Consult
          </a>
          <a
            href="/time-audit"
            class="inline-block rounded-full border border-hairline bg-white/35 px-7 py-3.5 text-[15px] font-semibold text-ink transition-colors hover:border-ink"
          >
            Take the Time Audit
          </a>
        </div>
        <p class="mt-3.5 text-sm text-fern">The audit takes 3 minutes. No email required.</p>
      </div>

      <!-- Sample scorecard (placeholder until a real anonymized one) -->
      <div class="rounded-[14px] border border-hairline bg-white/55 p-6 backdrop-blur-sm">
        <p class="mb-3 font-mono text-[11px] font-medium tracking-[0.14em] text-fern">
          SAMPLE SCORECARD
        </p>
        <div class="flex items-baseline justify-between gap-4 py-2.5 text-[13.5px] font-medium text-fern">
          <span>Revenue vs. plan</span>
          <span class="whitespace-nowrap font-mono text-sm text-ink">104%<i class="ml-1.5 text-[11px] not-italic text-moss">&#9650;</i></span>
        </div>
        <div class="flex items-baseline justify-between gap-4 border-t border-dashed border-hairline py-2.5 text-[13.5px] font-medium text-fern">
          <span>Gross margin</span>
          <span class="whitespace-nowrap font-mono text-sm text-ink">41%<i class="ml-1.5 text-[11px] not-italic text-moss">&#9650;</i></span>
        </div>
        <div class="flex items-baseline justify-between gap-4 border-t border-dashed border-hairline py-2.5 text-[13.5px] font-medium text-fern">
          <span>Owner hours in the weeds</span>
          <span class="whitespace-nowrap font-mono text-sm text-ink">6<i class="ml-1.5 text-[11px] not-italic text-moss">&#9660;</i></span>
        </div>
        <div class="flex items-baseline justify-between gap-4 border-t border-dashed border-hairline py-2.5 text-[13.5px] font-medium text-fern">
          <span>Decisions waiting on the owner</span>
          <span class="whitespace-nowrap font-mono text-sm text-ink">2<i class="ml-1.5 text-[11px] not-italic text-moss">&#9660;</i></span>
        </div>
        <div class="flex items-baseline justify-between gap-4 border-t border-dashed border-hairline py-2.5 text-[13.5px] font-medium text-fern">
          <span>Forecast accuracy</span>
          <span class="whitespace-nowrap font-mono text-sm text-ink">92%<i class="ml-1.5 text-[11px] not-italic text-moss">&#9650;</i></span>
        </div>
        <p class="font-display mt-3 text-sm italic text-fern">
          The weekly scorecard we build together. Your metrics, not these.
        </p>
      </div>
    </div>
  </section>

  <!-- THE PROBLEM -->
  <section class="border-b border-hairline px-6 py-16 md:py-20">
    <div class="mx-auto max-w-[1200px]">
      <p class="doc-label text-ink">The problem</p>
      <h2 class="font-display mt-7 text-3xl leading-tight text-ink md:text-4xl">
        You already know the signals.
      </h2>
      <div class="mt-7 grid gap-x-10 md:grid-cols-2">
        {[
          'Every decision still routes through you.',
          'Good hires bring you their problems instead of solving them.',
          "Revenue is flat, and nobody can tell you exactly why.",
          "You're working harder than last year and growing slower.",
        ].map((item) => (
          <div class="flex items-baseline gap-3.5 border-b border-dashed border-hairline py-3 text-[15.5px] font-semibold text-ink">
            <span class="cbx">X</span>
            {item}
          </div>
        ))}
      </div>
      <p class="mt-8 max-w-[60ch] text-lg leading-relaxed text-fern">
        None of that is a sales problem. It's an operating problem wearing a revenue costume — and you can't fix what you can't see clearly.
      </p>
    </div>
  </section>

  <!-- WHAT'S DIFFERENT -->
  <section class="border-b border-hairline bg-parchment-deep px-6 py-16 md:py-20">
    <div class="mx-auto max-w-[820px]">
      <p class="doc-label text-ink">What's different</p>
      <h2 class="font-display mt-7 text-3xl leading-tight text-ink md:text-4xl">
        Most people diagnose your business by talking to you. <em>I read your numbers.</em>
      </h2>
      <p class="mt-5 text-lg leading-relaxed text-fern">
        The coach runs a workshop. The consultant interviews your team. They work from what people are willing to say in a room. I go into your actual data — your CRM, your pipeline, how work gets delivered, where the money moves — and find the one thing quietly capping growth that nobody can see or won't say out loud.
      </p>
      <p class="mt-4 text-lg leading-relaxed text-fern">
        Then I don't hand you a slide deck. I build the way the business runs so it stops depending on you for everything.
      </p>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="border-b border-hairline px-6 py-16 md:py-20">
    <div class="mx-auto max-w-[820px]">
      <p class="doc-label text-ink">How it works</p>
      <h2 class="font-display mt-7 text-3xl leading-tight text-ink md:text-4xl">
        Same five moves, every time.
      </h2>
      <p class="mt-4 max-w-[54ch] text-lg leading-relaxed text-fern">
        The failure pattern in owner-led companies is remarkably consistent, whether you build houses, run a firm, or ship software.
      </p>
      <div class="route relative mt-9">
        {[
          { n: '1', t: 'Read the business', d: "I go through how the business actually makes money and where it leaks — from your real numbers, in a couple of weeks, not a two-day offsite." },
          { n: '2', t: "Find what's holding growth back", d: 'Every stuck company has one thing doing most of the damage. I name it and put it first, so we fix the cause instead of chasing symptoms.' },
          { n: '3', t: 'Put someone other than you in charge', d: "Every outcome gets an owner who isn't you. The context stuck in your head moves into the team." },
          { n: '4', t: 'Set the weekly rhythm', d: 'One scoreboard, one weekly meeting that runs itself. Decisions get made on schedule, from real numbers.' },
          { n: '5', t: 'Step out of the middle', d: 'The business runs the week without you. You go back to the work only you can do.' },
        ].map((stop, i, arr) => (
          <div class="relative grid grid-cols-[40px_1fr] gap-5 py-5">
            <div
              class:list={[
                'relative z-10 flex h-10 w-10 items-center justify-center rounded-full border-2 border-moss font-mono text-xs font-medium',
                i === arr.length - 1 ? 'bg-moss text-parchment' : 'bg-parchment text-moss',
              ]}
            >
              {stop.n}
            </div>
            <div>
              <h3 class="font-display text-[21px] text-ink">{stop.t}</h3>
              <p class="mt-1.5 max-w-[58ch] text-[15.5px] leading-relaxed text-fern">{stop.d}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- PROOF -->
  <section class="border-b border-hairline bg-parchment-deep px-6 py-16 md:py-20">
    <div class="mx-auto max-w-[1200px]">
      <p class="doc-label text-ink">Proof</p>
      <h2 class="font-display mt-7 text-3xl leading-tight text-ink md:text-4xl">
        What it looks like in a real business.
      </h2>

      <!-- Case study — PLACEHOLDER -->
      <div class="mt-8 rounded-[14px] border-2 border-dashed border-moss/50 bg-white/40 p-8">
        <p class="mb-4 font-mono text-[10.5px] font-medium uppercase tracking-[0.14em] text-rust">
          Placeholder · real case study coming
        </p>
        <div class="grid gap-6 md:grid-cols-3">
          <div>
            <p class="font-mono text-[11px] font-medium uppercase tracking-[0.12em] text-moss">The business</p>
            <p class="mt-2 leading-relaxed text-ink">$[X]M [vertical] company. Growing, but the owner was in every decision and margin was slipping.</p>
          </div>
          <div>
            <p class="font-mono text-[11px] font-medium uppercase tracking-[0.12em] text-moss">What I found</p>
            <p class="mt-2 leading-relaxed text-ink">The constraint wasn't sales. It was [the real thing] — hiding in the [system] data, where no one was looking.</p>
          </div>
          <div>
            <p class="font-mono text-[11px] font-medium uppercase tracking-[0.12em] text-moss">The result</p>
            <p class="mt-2 leading-relaxed text-ink">[Metric] moved from [X] to [Y] in [timeframe]. The owner stopped being the bottleneck.</p>
          </div>
        </div>
      </div>

      <!-- Quotes — PLACEHOLDER -->
      <div class="mt-6 grid gap-6 md:grid-cols-2">
        <div class="rounded-[14px] border border-hairline bg-white/50 p-7">
          <p class="mb-3 font-mono text-[10.5px] font-medium uppercase tracking-[0.14em] text-rust">Placeholder · real quote coming</p>
          <blockquote class="font-display text-[18px] italic leading-normal text-ink">
            "A real sentence from a real client, with their real name and company under it. One honest line beats a page of polish."
          </blockquote>
          <cite class="mt-3 block font-mono text-[11px] not-italic text-fern">NAME — ROLE, COMPANY</cite>
        </div>
        <div class="rounded-[14px] border border-hairline bg-white/50 p-7">
          <p class="mb-3 font-mono text-[10.5px] font-medium uppercase tracking-[0.14em] text-rust">Placeholder · real quote coming</p>
          <blockquote class="font-display text-[18px] italic leading-normal text-ink">
            "A second quote, ideally from a different kind of business, so a visitor sees the range holds up."
          </blockquote>
          <cite class="mt-3 block font-mono text-[11px] not-italic text-fern">NAME — ROLE, COMPANY</cite>
        </div>
      </div>

      <!-- Compact operator credibility (not a brag section) -->
      <p class="mt-8 max-w-[70ch] text-[15px] leading-relaxed text-fern">
        Behind Growth Map: <span class="font-semibold text-ink">Cameron Oliver</span> — a decade operating inside fast-growing companies, from employee #3 to 100+, usually the person leaders called when things started breaking.
        <a href="/about" class="font-semibold text-moss transition-colors hover:text-ink">More on that →</a>
      </p>
    </div>
  </section>

  <!-- WHERE TO START -->
  <section class="border-b border-hairline px-6 py-16 md:py-20">
    <div class="mx-auto max-w-[1200px]">
      <p class="doc-label text-ink">Where to start</p>
      <h2 class="font-display mt-7 text-3xl leading-tight text-ink md:text-4xl">
        Start with a Diagnostic.
      </h2>

      <!-- Front-door offer -->
      <div class="mt-8 rounded-[14px] border-[1.5px] border-moss bg-white/70 p-8 md:p-10">
        <div class="grid gap-8 md:grid-cols-[1.4fr_1fr] md:gap-12">
          <div>
            <h3 class="font-display text-[26px] text-ink">The Diagnostic</h3>
            <p class="mt-3 text-lg font-semibold leading-relaxed text-ink">
              In two weeks, you'll know exactly what's capping your growth — proven from your own numbers, not a hunch.
            </p>
            <p class="mt-4 leading-relaxed text-fern">
              Give me read-only access to your systems. I go in, find the one constraint doing the most damage, and hand you a short written brief: what it is, what it's costing you, and the moves that fix it. Yours to run with, with me or without me.
            </p>
            <p class="mt-4 text-[15.5px] font-semibold leading-relaxed text-moss">
              If I don't find something worth more than the fee, you don't pay.
            </p>
            <a
              href="https://calendar.notion.so/meet/cameronearloliver/qc3y04oud"
              target="_blank"
              rel="noopener noreferrer"
              class="mt-6 inline-block rounded-full border border-ink bg-ink px-7 py-3.5 text-[15px] font-semibold text-parchment transition-opacity hover:opacity-90"
            >
              Book a Free Consult
            </a>
            <p class="mt-3 font-mono text-xs font-medium tracking-[0.1em] text-fern">TWO WEEKS · ONE FIXED FEE</p>
          </div>
          <div class="md:border-l md:border-hairline md:pl-10">
            <p class="font-mono text-[11px] font-medium uppercase tracking-[0.14em] text-fern">If you want me to keep going</p>
            <div class="mt-4">
              <h4 class="font-display text-[19px] text-ink">The Build</h4>
              <p class="mt-1.5 text-[14.5px] leading-relaxed text-fern">
                90 days to install the fix the Diagnostic found. Your team runs the weekly without you by day 90, or I keep going until they do. Three at a time.
              </p>
            </div>
            <div class="mt-6 border-t border-dashed border-hairline pt-6">
              <h4 class="font-display text-[19px] text-ink">Operating Partner</h4>
              <p class="mt-1.5 text-[14.5px] leading-relaxed text-fern">
                Monthly, after a Build. I stay on, run the weekly, and keep pushing the next improvement.
              </p>
            </div>
          </div>
        </div>
      </div>

      <p class="mt-7 max-w-[64ch] text-[15px] leading-relaxed text-fern">
        Every engagement is one fixed fee, quoted to the problem — not an hourly meter. You shouldn't have to make a money decision every time you need to call me.
      </p>
    </div>
  </section>

  <!-- FIELD NOTES -->
  <section class="px-6 py-16 md:py-20">
    <div class="mx-auto max-w-[1200px]">
      <p class="doc-label text-ink">Field notes</p>
      <h2 class="font-display mt-7 text-3xl leading-tight text-ink md:text-4xl">
        How I think about this work.
      </h2>
      <p class="mt-4 max-w-[56ch] text-lg leading-relaxed text-fern">
        The clearest read on whether I'm the right person is how I think. I write about operations, growth, and getting out of your own way — from inside the work.
      </p>
      <div class="mt-8">
        {posts.map((post) => (
          <a
            href={`/blog/${post.id}`}
            class="group flex items-baseline justify-between gap-6 border-t border-hairline py-5 no-underline"
          >
            <span class="font-display text-[21px] text-ink transition-colors group-hover:text-moss">
              {post.data.title}
            </span>
            <span class="flex-shrink-0 font-mono text-xs font-medium text-moss">READ →</span>
          </a>
        ))}
      </div>
      <a href="/blog" class="mt-6 inline-block text-[15px] font-semibold text-moss transition-colors hover:text-ink">
        All field notes →
      </a>
    </div>
  </section>
</Layout>

<style>
  /* dotted route rail connecting the how-it-works steps */
  .route::before {
    content: '';
    position: absolute;
    left: 19px;
    top: 24px;
    bottom: 24px;
    border-left: 2px dashed var(--color-moss);
    opacity: 0.45;
  }
</style>
